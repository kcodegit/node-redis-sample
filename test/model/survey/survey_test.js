// imports and vars
var chai = require('chai');
chai.use(require('chai-as-promised'));
var assert = chai.assert,
  expect = chai.expect,
  should = chai.should(),
  Survey = require('../../../src/model/survey/Survey'),
  p = console.log;

describe('Survey', function () {
  describe('constructor()', function () {
    it('should DO SOMETHING', function () {

    });
  });
});