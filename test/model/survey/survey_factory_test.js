'use strict';

// imports and vars
var chai = require('chai');
chai.use(require('chai-as-promised'));
var assert = chai.assert,
  expect = chai.expect,
  should = chai.should(),
  SurveyFactory = require('../../../src/model/survey/SurveyFactory'),
  p = console.log;

describe('SurveyFactory', function () {
  describe('getInstanceFromDBResult()', function () {
    it('should DO SOMETHING', function () {

    });
  });
});