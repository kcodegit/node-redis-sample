'use strict';

// imports and vars
var chai = require('chai');
chai.use(require('chai-as-promised'));
var assert = chai.assert,
  expect = chai.expect,
  should = chai.should(),
  SubmissionRepository = require('../../../src/model/submission/SubmissionRepository'),
  p = console.log;

describe('SubmissionRepository', function () {
  describe('add()', function () {
    it('should DO SOMETHING', function () {

    });
  });
});