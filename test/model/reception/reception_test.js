'use strict';

// imports and vars
var chai = require('chai');
chai.use(require('chai-as-promised'));
var assert = chai.assert,
  expect = chai.expect,
  should = chai.should(),
  Reception = require('../../../src/model/reception/Reception'),
  p = console.log;

describe('Reception', function () {
  describe('receiveSubmission()', function () {
    it('should DO SOMETHING', function () {

    });
  });
  describe('increRedis()', function () {
    it('should DO SOMETHING', function () {

    });
  });
  describe('getCount()', function () {
    it('should DO SOMETHING', function () {

    });
  });
});
